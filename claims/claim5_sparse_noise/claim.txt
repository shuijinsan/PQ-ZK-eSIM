Paper: sparse-noise / multi-norm validation.

Claim: structured degradation of y_pub is tested against the actual verifier while preserving the lattice relation. The experiment records which norm check rejects each trial.

Experiment:
  qemu-aarch64-static -L /usr/aarch64-linux-gnu \
      artifact/terminal/euicc/build/arm64/bench_pqzkesim_comprehensive --only sparse

Expected outputs:
  sparse_noise_attack_results.csv
  sparse_noise_norm_breakdown.csv

The rho=0.78 value is only the ell_2 geometric reference threshold for the stated sparsification model; the full verifier also applies ell_infinity and ell_1 checks. Honest false-rejection must be reported empirically and is not defined to be exactly zero.
