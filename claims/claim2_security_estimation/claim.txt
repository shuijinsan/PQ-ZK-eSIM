Paper: Figure 4, Section 7.1 (Instantiation / concrete lattice-attack estimation)

Claim: The artifact reproduces the HNF-MSIS lattice-estimator result used in
Figure 4. At the paper's extraction bound gamma_ext = 71400, the pinned
lattice-estimator configuration reports approximately 142.6 classical bits
and 135.1 quantum bits. The paper also reports MLWE costs of 172.1 classical bits and 162.9 quantum
bits. Those MLWE inputs and results are documented below for paper alignment;
the Claim 2 run script reproduces the HNF-MSIS sweep only.

MLWE input:
  N=256, q=8380417, k=3, m=8
  coefficient-view dimension n=(m-k)N=1280
  samples=kN=768
  secret=uniform ternary, error=uniform ternary
  reported minimum: dual_hybrid, 172.1 classical bits, 162.9 quantum bits

HNF-MSIS input:
  N=256, q=8380417, k=3, m=8
  coefficient-view rows=768, columns=2304
  norm=infinity, length_bound=71400
  reported minimum: 142.6 classical bits, 135.1 quantum bits

Experiment: lattice-estimator sweep over the HNF-MSIS length_bound
(40000 to 200000), reporting classical (MATZOV) and quantum (LaaMosPol14)
bits for each point.

Input: none (the instance is derived from src/internal/params.h)

Expected output:
  msis_estimator_sweep.csv
    columns: length_bound, classical_bits, quantum_bits, bkz_block_size
  fig_lattice_estimator.png  (Figure 4)

Runtime: ~1 min

Resources: 1 CPU core, <1 GB RAM, <10 MB disk

Dependency: lattice-estimator, commit 6019056
(github.com/malb/lattice-estimator), Python 3, fpylll, SageMath.

Tolerance:
  - Deterministic: for a fixed instance, cost model, and estimator commit the
    reported bit counts are exact; there is no run-to-run variation.
