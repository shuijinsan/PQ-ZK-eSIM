Paper: Figure 6, Appendix D (Resistance to Real-World Attacks)

Claim: sliding-window MAC resynchronization recovers from counter
desynchronization within the window (w_sync up to 32) and rejects beyond it.

Experiment (QEMU ARM64 emulation):
  qemu-aarch64-static -L /usr/aarch64-linux-gnu \
      artifact/terminal/euicc/build/arm64/bench_pqzkesim_comprehensive --only sliding

Input: none

Expected output: sliding_window_resync_results.csv
  columns: window_size, sync_depth, success_rate, avg_mac_us, avg_total_us

Measured:
  resynchronization succeeds iff Delta <= W across the tested grid;
  W=32, Delta=32: MAC search about 2.96 ms;
  total in-window latency about 6.8 ms;
  Delta=64: outside-window failure about 3.0 ms (lattice verification skipped);
  expected MAC-search reference: (W+1) x 71.0 us.

Runtime: ~1-2 min

Resources: 1 CPU core, <100 MB RAM, <1 MB disk

Tolerance:
  - success_rate: deterministic (1.0 within window, 0.0 beyond window).
  - timing: faster passes; slower up to +100% (2x).

Artifact commit: 5db8589f796d5c5959dd889c44295befe357a3e3
