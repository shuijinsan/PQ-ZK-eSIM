# AArch64 cross-compilation
- Compiler: `aarch64-linux-gnu-gcc`
- CMake toolchain: `../euicc/aarch64-toolchain.cmake`
- Build output: `../euicc/build/arm64/`
- Execute with `qemu-aarch64-static`.

QEMU results are emulated ARM64 workload measurements, not real hardware benchmarks.
